import './home.scss';

const Home = {
  render: async () => {
    const view = /* html */ `
      <div class="container home-container">
      </div>
    `;
    return view;
  },
  after_render: async () => {},
};

export default Home;
