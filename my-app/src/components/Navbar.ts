const Navbar = {
  render: async () => {
    const view = /* html */ `
              <div class="container">
    
              </div>
      `;
    return view;
  },
  after_render: async () => {},
};

export default Navbar;
